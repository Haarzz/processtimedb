BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[arduino] DROP CONSTRAINT [arduino_assigned_transaction_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
